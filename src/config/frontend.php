<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Frontend app urls
    |--------------------------------------------------------------------------
    |
    | When you have a separate frontend, specify the urls here for
    | email verification redirect url and reset password redirect url.
    |
    */

    'verification_url' => env('VERIFICATION_URL', null),


    'reset_password_url' => env('RESET_PASSWORD_URL', null),
];
