<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Frontend URL
    |--------------------------------------------------------------------------
    |
    | When you have a separate frontend, specify the url here.
    | Email verification will use this config to redirect the users
    | to the specified url instead of your backend url.
    |
    */

    'app_url' => env('FRONTEND_APP_URL', null)
];
